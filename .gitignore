# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Build artifacts
tmp/
# Dev settings directory, seeded by `make dev` (wavehouse bootstrap settings)
/settings/
dist/

# Build directories
build/
bin/
# `go build ./cmd/wavehouse` with no -o drops the binary in the repo root
/wavehouse

# Stray binaries from `go build ./scripts/<name>` — Go helpers under scripts/
# are meant to be run via `go run` (the Makefile drives them that way), but a
# direct `go build ./scripts/cov` would drop the binary at the repo root
# because Go uses the package's directory basename as the output name.
# `go build ./...` of multiple main packages already discards outputs, so
# this only catches the explicit single-package case. One line per helper.
/orchestrator
/cov

# Data directories (runtime)
data/

# Test artifacts
*.test
*.out

# Coverage outputs
coverage.txt
coverage.out
# vitest's default coverage dir, used when `pnpm test:coverage` runs
# without SDK_COVERAGE_DIR set (i.e. directly inside clients/ts/).
coverage/
clients/ts/coverage/

# Local Tooling directory
tools/
.bin/

# Dependency directories
vendor/
node_modules/

# IDE / Editor
.idea/
*.swp
*.swo
*~

# Claude Code local state (per-user; not for committing)
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock

# Local git worktrees
.worktrees/

# OS files
.DS_Store
Thumbs.db

# Environment files
.env
.env.local
.env.*.local

# Debug
__debug_bin*

# Docs site build outputs
docs/.astro/
docs/.dev-dist/
docs/.env.production
docs/.wrangler/
docs/dist/

# pnpm
pnpm-debug.log*

# local config files
.config.local.yaml

# Local cache validation outputs
.validation/
